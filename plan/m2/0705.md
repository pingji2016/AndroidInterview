### 7月5日（周日）Day 41 — 密集冲刺：JSON 协议 + ReAct 骨架
| 时段 | 任务 | 产出 |
|:---|:---|:---|
| 2h | **升级为 JSON 协议**：从硬编码改为 JSON Schema 解析 | JSON 解析代码 |
| 1h | 完善 `ToolManager`：注册/查找/执行生命周期，加错误处理 | 重构后代码 |
| 1h | **ReAct 循环设计**：理解 ReAct 思路，画流程图 | ReAct 流程图 |
| 2h | **实现 ReAct 骨架**：`while(hasNext) { think → act → observe → think... }` | 循环代码框架 |

**产出物**：JSON 协议 + ToolManager 重构 + ReAct 流程图 + 骨架代码

---

## 一、从硬编码到 JSON 协议

### 为什么需要 JSON 协议？

昨天（7/4）用的是硬编码解析：
```kotlin
// 硬编码方式（问题：不灵活，只能匹配固定关键词）
if (output.contains("sendNotification")) { notificationTool.execute(...) }
if (output.contains("adjustBrightness")) { brightnessTool.execute(...) }
```

**问题**：
- 添加新 Tool 需要改代码
- 模型输出格式稍有变化就匹配不上
- 无法传递结构化参数

**升级为 JSON 协议**：让模型输出标准 JSON 格式，统一解析。

### 协议设计

```json
// 模型输出格式标准
{
  "action": "sendNotification",    // 工具名称
  "params": {                      // 参数
    "title": "任务完成",
    "content": "你的 Agent 任务已完成"
  }
}
```

### Tool Schema 定义（告诉模型有哪些工具可用）

```kotlin
/**
 * Tool Schema：描述一个工具的元数据
 * 会被序列化为 JSON 拼入 Prompt，告诉模型可用的工具
 */
data class ToolSchema(
    val name: String,           // 工具名，如 "sendNotification"
    val description: String,    // 工具描述，如 "发送系统通知"
    val parameters: List<ParameterSchema> // 参数列表
)

data class ParameterSchema(
    val name: String,
    val type: String,           // "string", "number", "boolean"
    val description: String,
    val required: Boolean = true
)
```

### JSON 解析器实现

```kotlin
// app/src/main/java/com/example/androidagent/tool/JsonActionParser.kt

package com.example.androidagent.tool

import org.json.JSONObject

/**
 * 解析模型输出的 JSON，提取动作和参数
 */
class JsonActionParser : ActionParser {

    /**
     * 尝试从模型输出中提取 JSON 动作
     *
     * 支持两种格式：
     * 1. 纯 JSON：{"action": "...", "params": {...}}
     * 2. 文本中嵌入 JSON：...{"action": "...", "params": {...}}... 
     */
    override fun parse(output: String): ParsedAction? {
        // 尝试提取 JSON 块（处理模型输出中夹杂着其他文本的情况）
        val jsonStr = extractJsonBlock(output) ?: return null
        
        return try {
            val json = JSONObject(jsonStr)
            val action = json.optString("action", "")
            if (action.isBlank()) return null
            
            val params = json.optJSONObject("params")
            val paramsMap = mutableMapOf<String, Any>()
            params?.let { p ->
                p.keys().forEach { key ->
                    paramsMap[key] = p.get(key)
                }
            }
            
            ParsedAction(action, paramsMap)
        } catch (e: Exception) {
            null  // 解析失败，返回 null
        }
    }

    /**
     * 从文本中提取第一个 JSON 对象块
     */
    private fun extractJsonBlock(text: String): String? {
        val start = text.indexOf('{')
        val end = text.lastIndexOf('}')
        if (start == -1 || end == -1 || end <= start) return null
        return text.substring(start, end + 1)
    }
}

/**
 * 解析结果
 */
data class ParsedAction(
    val action: String,                    // 工具名
    val params: Map<String, Any> = emptyMap()  // 参数
)

/**
 * 动作解析器接口（支持多种解析策略）
 */
interface ActionParser {
    fun parse(output: String): ParsedAction?
}
```

### 构建 Tool Schema 的 Prompt

```kotlin
/**
 * 构建包含 Tool Schema 的 ReAct Prompt
 *
 * 这个 Prompt 告诉模型：
 * 1. 可用的工具有哪些（name + description + parameters）
 * 2. 输出格式要求（JSON）
 * 3. 思考步骤要求（Thought → Action → Observation → Final Answer）
 */
fun buildReActPrompt(userInput: String, tools: List<AgentTool>): String {
    val toolDescriptions = tools.joinToString("\n") { tool ->
        val schema = tool.getSchema()
        val params = schema.parameters.joinToString(", ") { p ->
            "${p.name} (${p.type}): ${p.description}"
        }
        "- ${schema.name}: ${schema.description}，参数: [$params]"
    }

    return """
<|im_start|>system
你是 Android 设备上的 AI 助手。你可以通过调用工具来控制手机功能。

可用的工具：
$toolDescriptions

请按照以下格式回复：
{
  "thought": "你的思考过程",
  "action": "工具名称",
  "params": { ... }
}

执行完毕后，回复：
{
  "thought": "你的思考过程",
  "action": "Final Answer",
  "answer": "最终回复给用户的内容"
}
<|im_end|>
<|im_start|>user
$userInput<|im_end|>
<|im_start|>assistant
    """.trimIndent()
}
```

### JSON 协议 vs 硬编码对比

| 维度 | 硬编码 | JSON 协议 |
|:---|:---|:---|
| **灵活性** | 低，加 Tool 改代码 | 高，加 Tool 只需注册 |
| **参数传递** | 字符串拼接 | 结构化 JSON |
| **容错性** | 关键词匹配，易错 | JSON 解析，容错更好 |
| **扩展性** | ❌ 差 | ✅ 好 |

---

## 二、完善 ToolManager

### ToolManager 完整实现

```kotlin
// app/src/main/java/com/example/androidagent/tool/ToolManager.kt

package com.example.androidagent.tool

/**
 * 工具管理器：负责工具的注册、查找、执行
 *
 * 职责：
 * 1. 注册所有可用工具
 * 2. 根据名称查找工具
 * 3. 执行指定工具
 * 4. 统一错误处理
 */
class ToolManager {

    private val tools = mutableMapOf<String, AgentTool>()

    /**
     * 注册一个工具
     */
    fun register(tool: AgentTool) {
        tools[tool.getSchema().name] = tool
    }

    /**
     * 批量注册工具
     */
    fun registerAll(tools: List<AgentTool>) {
        tools.forEach { register(it) }
    }

    /**
     * 获取所有可用的工具 Schema（用于构建 Prompt）
     */
    fun getAllSchemas(): List<ToolSchema> {
        return tools.values.map { it.getSchema() }
    }

    /**
     * 执行工具
     *
     * @param action 解析后的动作
     * @return 执行结果描述（用于 Observation 回填到 Prompt）
     */
    suspend fun execute(action: ParsedAction): ToolResult {
        val tool = tools[action.action]
            ?: return ToolResult.failure("未知工具: ${action.action}")

        return try {
            tool.execute(action.params)
        } catch (e: Exception) {
            ToolResult.failure("工具执行异常: ${e.message}")
        }
    }

    /**
     * 获取工具数量
     */
    fun size(): Int = tools.size
}

/**
 * 工具执行结果
 */
data class ToolResult(
    val success: Boolean,
    val message: String,
    val data: Map<String, Any>? = null
) {
    companion object {
        fun success(message: String, data: Map<String, Any>? = null) =
            ToolResult(true, message, data)

        fun failure(message: String) =
            ToolResult(false, message)
    }

    /**
     * 转成 Observation 字符串（回填到 Prompt）
     */
    fun toObservation(): String {
        return if (success) {
            "执行成功: $message"
        } else {
            "执行失败: $message"
        }
    }
}
```

### AgentTool 接口重构

```kotlin
// app/src/main/java/com/example/androidagent/tool/AgentTool.kt

package com.example.androidagent.tool

/**
 * 工具接口：所有系统工具需要实现的接口
 */
interface AgentTool {

    /**
     * 获取工具的 Schema 描述
     */
    fun getSchema(): ToolSchema

    /**
     * 执行工具
     *
     * @param params 参数映射
     * @return 执行结果
     */
    suspend fun execute(params: Map<String, Any>): ToolResult
}
```

### SendNotificationTool 用新接口重构

```kotlin
// app/src/main/java/com/example/androidagent/tool/impl/SendNotificationTool.kt

package com.example.androidagent.tool.impl

import android.content.Context
import androidx.core.app.NotificationCompat
import androidx.core.app.NotificationManagerCompat
import com.example.androidagent.tool.*

class SendNotificationTool(
    private val context: Context
) : AgentTool {

    override fun getSchema(): ToolSchema {
        return ToolSchema(
            name = "sendNotification",
            description = "发送一条系统通知",
            parameters = listOf(
                ParameterSchema("title", "string", "通知标题，如'任务完成'"),
                ParameterSchema("content", "string", "通知内容，如'你的下载已完成'")
            )
        )
    }

    override suspend fun execute(params: Map<String, Any>): ToolResult {
        val title = params["title"] as? String ?: return ToolResult.failure("缺少参数: title")
        val content = params["content"] as? String ?: return ToolResult.failure("缺少参数: content")

        val notification = NotificationCompat.Builder(context, CHANNEL_ID)
            .setSmallIcon(android.R.drawable.ic_dialog_info)
            .setContentTitle(title)
            .setContentText(content)
            .setPriority(NotificationCompat.PRIORITY_DEFAULT)
            .setAutoCancel(true)
            .build()

        NotificationManagerCompat.from(context).notify(
            System.currentTimeMillis().toInt(),
            notification
        )

        return ToolResult.success("通知已发送: $title - $content")
    }

    companion object {
        private const val CHANNEL_ID = "agent_channel"
        fun createNotificationChannel(context: Context) {
            val channel = android.app.NotificationChannel(
                CHANNEL_ID,
                "Agent 通知",
                android.app.NotificationManager.IMPORTANCE_DEFAULT
            )
            val manager = context.getSystemService(Context.NOTIFICATION_SERVICE)
                as android.app.NotificationManager
            manager.createNotificationChannel(channel)
        }
    }
}
```

---

## 三、ReAct 循环设计

### ReAct 核心流程

```
ReAct = Reasoning + Acting

每一轮循环：
  1. Thought（思考）   → 模型分析当前状态，决定下一步做什么
  2. Action（行动）    → 模型选择要调用的工具
  3. Action Input（参数） → 模型的参数（JSON）
  4. Observation（观察） → 工具执行结果
  5. 回到 1，或输出 Final Answer
```

### ReAct 流程图

```
用户输入 "帮我调亮屏幕并发送通知"
        │
        ▼
┌─────────────────────────────────┐
│  Step 1: Thought + Action       │
│                                 │
│  Thought: 用户想调亮屏幕         │
│  Action: adjustBrightness       │
│  Params: {"level": 80}          │
└─────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────┐
│  Step 2: Observation            │
│                                 │
│  亮度已调至 80%                 │
│  （Observation 回填到 Prompt）   │
└─────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────┐
│  Step 3: Thought + Action       │
│                                 │
│  Thought: 亮度已调好，接下来     │
│  发送通知                       │
│  Action: sendNotification       │
│  Params: {"content": "已调亮"}   │
└─────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────┐
│  Step 4: Observation            │
│                                 │
│  通知已发送                      │
└─────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────┐
│  Step 5: Final Answer           │
│                                 │
│  "已完成！亮度已调至 80%，并     │
│   已发送通知。"                 │
└─────────────────────────────────┘
```

### ReAct Prompt 模板

```kotlin
/**
 * 构建 ReAct 循环的完整 Prompt
 *
 * 这个 Prompt 包含：
 * 1. 系统指令（告诉模型它是 Agent）
 * 2. 可用工具列表（Schema）
 * 3. 历史对话（Thought/Action/Observation 循环）
 * 4. 当前用户输入
 */
fun buildReActPrompt(
    userInput: String,
    toolSchemas: List<ToolSchema>,
    history: List<ReActStep> = emptyList()
): String {
    // 1. 工具描述
    val toolDesc = toolSchemas.joinToString("\n") { schema ->
        val params = schema.parameters.joinToString(", ") { p ->
            "${p.name} (${p.type}): ${p.description}"
        }
        "- ${schema.name}: ${schema.description}，参数: [$params]"
    }

    // 2. 历史步骤
    val historyStr = history.joinToString("\n") { step ->
        """
Thought: ${step.thought}
Action: ${step.action}
Action Input: ${step.actionInput}
Observation: ${step.observation}
        """.trimIndent()
    }

    return """
<|im_start|>system
你是一个 Android 手机上的 AI 助手。你需要通过思考(Thought)和行动(Action)来完成用户的指令。

可用工具：
$toolDesc

每次回复请使用以下 JSON 格式：
{
  "thought": "你的思考过程",
  "action": "工具名称 或 Final Answer",
  "params": { ... },
  "answer": "如果 action 是 Final Answer，这里填写回复内容"
}
<|im_end|>

${if (historyStr.isNotBlank()) """
<|im_start|>history
$historyStr
<|im_end|>
""" else ""}

<|im_start|>user
$userInput
<|im_end|>
<|im_start|>assistant
    """.trimIndent()
}

/**
 * ReAct 循环中的一步
 */
data class ReActStep(
    val thought: String,
    val action: String,
    val actionInput: String,
    val observation: String
)
```

---

## 四、ReAct 骨架实现

```kotlin
// app/src/main/java/com/example/androidagent/agent/ReActAgent.kt

package com.example.androidagent.agent

import com.example.androidagent.inference.LlamaInference
import com.example.androidagent.tool.*
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext

/**
 * ReAct Agent 核心实现
 *
 * 循环步骤：
 * 1. 构建 Prompt（包含 Schema + 历史）
 * 2. LLM 推理 → 输出 JSON
 * 3. 解析 JSON → 提取 action + params
 * 4. 如果 action == "Final Answer" → 返回结果
 * 5. 否则执行 Tool → Observation → 回填 Prompt → 回到 1
 */
class ReActAgent(
    private val inference: LlamaInference,
    private val toolManager: ToolManager,
    private val parser: ActionParser = JsonActionParser()
) {
    companion object {
        private const val MAX_STEPS = 5  // 最大循环步数，防止死循环
    }

    /**
     * 执行 ReAct 循环
     *
     * @param userInput 用户输入
     * @param onStep 每一步的回调（用于 UI 展示）
     * @return 最终回复
     */
    suspend fun execute(
        userInput: String,
        onStep: (ReActStep) -> Unit = {}
    ): String = withContext(Dispatchers.IO) {

        val history = mutableListOf<ReActStep>()

        for (step in 1..MAX_STEPS) {
            // 1. 构建 Prompt
            val prompt = buildReActPrompt(
                userInput = userInput,
                toolSchemas = toolManager.getAllSchemas(),
                history = history
            )

            // 2. LLM 推理
            val output = inference.runInference(prompt)
                .getOrElse { return@withContext "推理失败: ${it.message}" }

            // 3. 解析输出
            val parsed = parser.parse(output)

            if (parsed == null) {
                // 解析失败，让模型重试
                history.add(ReActStep(
                    thought = "解析失败",
                    action = "retry",
                    actionInput = output,
                    observation = "输出格式错误，请使用 JSON 格式回复"
                ))
                continue
            }

            // 4. 检查是否 Final Answer
            if (parsed.action == "Final Answer") {
                val answer = parsed.params["answer"] as? String
                    ?: parsed.params["thought"] as? String
                    ?: "任务完成"
                return@withContext answer
            }

            // 5. 执行工具
            val result = toolManager.execute(parsed)

            // 6. 记录这一步
            val reactStep = ReActStep(
                thought = parsed.params["thought"] as? String ?: "",
                action = parsed.action,
                actionInput = parsed.params.toString(),
                observation = result.toObservation()
            )
            history.add(reactStep)
            onStep(reactStep)
        }

        // 超过最大步数
        return@withContext "抱歉，执行步骤过多未能完成，请简化指令重试"
    }
}
```

### ReAct 循环状态图

```
execute() 调用开始
      │
      ▼
┌─────────────┐     ┌──────────────────┐
│ 构建 Prompt  │────→│  LLM 推理         │
└─────────────┘     └────────┬─────────┘
                             │
                             ▼
                      ┌──────────────────┐
                      │  解析 JSON 输出   │
                      └────────┬─────────┘
                             │
                    ┌────────┴────────┐
                    ▼                 ▼
              ┌──────────┐    ┌──────────────┐
              │ Final     │    │ Tool 调用     │
              │ Answer?   │    │              │
              └────┬─────┘    └──────┬───────┘
                   │                 │
                  Yes                │
                   │                 ▼
                   ▼          ┌──────────────┐
              ┌──────────┐    │ Observation  │
              │ 返回结果  │    │ 回填 Prompt   │
              └──────────┘    └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  step > 5?   │
                              └──┬───────┬───┘
                               No      Yes
                               │       │
                               └─←─────┘
                                       ▼
                               ┌──────────────┐
                               │ 返回超时提示  │
                               └──────────────┘
```

---

## 产出物确认

- [x] `JsonActionParser.kt` — JSON 协议解析器，支持从文本中提取 JSON 块
- [x] `ToolSchema` / `ParameterSchema` — 工具 Schema 定义，用于构建 Prompt
- [x] `ToolManager.kt` — 完整的工具管理器（注册/查找/执行/错误处理）
- [x] `AgentTool` 接口重构 — 返回 `ToolResult` 统一结果格式
- [x] `ReActAgent.kt` — ReAct 循环核心实现（Thought → Action → Observation 循环）
- [x] `buildReActPrompt()` — 构建包含 Schema + 历史的 Prompt
- [x] ReAct 流程图 — 清晰的步骤图
- [x] ReAct 状态图 — 代码执行流程
