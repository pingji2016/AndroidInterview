### 5月26日（周二）Day 1
| 时段 | 任务 | 产出 |
|------|------|------|
| 30min | 复习 **synchronized**：对象锁 vs 类锁、可重入性、与 Lock 的区别 | 写一段代码演示死锁，口述原因 |
| 30min | 复习 **volatile**：可见性、禁止指令重排、不保证原子性 | 用代码演示 volatile 无法解决 i++ 问题 |
| 60min | 练习：手写单例（双重检查锁、静态内部类、枚举） | 3种单例的代码截图 |

**产出物**：一个笔记文件，包含 synchronized/volatile 对比表格 + 3种单例代码


import java.util.concurrent.*;
import java.util.concurrent.atomic.AtomicInteger;

public class demo0525 {
    private static volatile int x = 0;

    public static void main(String[] args) throws InterruptedException {
        ExecutorService executorService = Executors.newCachedThreadPool();
        for (int i = 0; i < 1000; i++) {
            executorService.submit(() -> {
                x++;
            });
        }

        executorService.shutdown();
        executorService.awaitTermination(10, TimeUnit.SECONDS);

        System.out.println(x);
    }
}



import java.util.Objects;
import java.util.concurrent.*;
import java.util.concurrent.atomic.AtomicInteger;

public class demo0525 {
    private static Object o1 = new Object();
    private static Object o2 = new Object();


    public static void main(String[] args) throws InterruptedException {
        ExecutorService executor = Executors.newFixedThreadPool(2);
        executor.submit(() -> {
            synchronized (o1) {
                System.out.println("线程1");
                synchronized (o2) {
                    System.out.println("线程1 哈哈哈");
                }
            }
        });

        executor.submit(() -> {
            try {
                System.out.println("线程2");
                o2.wait();
            } catch (InterruptedException e) {
                throw new RuntimeException(e);
            }
            o1.notify();
        });


        executor.shutdown();
        executor.awaitTermination(10, TimeUnit.SECONDS);

        System.out.println("结束");
    }
}


synchronized (lock) {
    while (condition) {
        lock.wait();      // 条件不满足，释放锁并等待
    }
    // 条件满足，继续执行
}

// 另一个线程
synchronized (lock) {
    // 改变条件
    lock.notify();        // 通知等待的线程
}



public class demo0525 {

    public static void main(String[] args) throws InterruptedException {

    }

    public static demo0525 getInstance() {
        return Instance.de;
    }

    static class Instance {
        private static  final demo0525 de = new demo0525();
    }
}


public class demo {
    private demo() {

    }

    static volatile demo dd;
    public static demo getInstance() {
        if(dd == null) {
            synchronized (demo.class) {
                if (dd == null) {
                    dd = new demo();
                }
            }
        }
        return dd;
    }
}
